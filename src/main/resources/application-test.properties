# Filename: src/main/resources/application-test.properties

# --- DATABASE CONFIG FOR TESTING ---
# Directly set the local database URL for test stability
spring.datasource.url=jdbc:postgresql://localhost:5432/krishi_sakhi_db
spring.datasource.username=postgres
spring.datasource.password=admin

# Ensure test context doesn't try to deploy all production services
# Use 'none' or 'create-drop' for tests
spring.jpa.hibernate.ddl-auto=create-drop

# --- SECURITY FALLBACKS FOR TESTING ---
# Ensure JWT is defined for tests that rely on the SecurityConfig loading
jwt.secret=TestSecretKeyForTestingContext